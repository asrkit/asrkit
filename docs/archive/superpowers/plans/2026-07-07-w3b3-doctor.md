# W3b-3 `asrkit doctor` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `asrkit doctor [--net]` 离线体检(版本/引擎/密钥/models 目录/config)+ opt-in 网络可达,退出码在硬问题时非零。

**Architecture:** 新 `doctor.py`(`diagnose(net) -> list[Check]`,试写探针 + config 直读 + 密钥只报 vendor);`cli.py` 加 `doctor` 子命令渲染 + 定退出码。纯只读诊断,零新依赖。

**Tech Stack:** Python 3.9+;stdlib `tempfile`/`json`/`urllib`;pytest + monkeypatch。

## Global Constraints

- 版本号**不动**(`__version__` 保持 `0.5.2`);发版由人类定。
- **零新增运行时依赖**。
- 终端/帮助**英文**;注释**中文**。
- **只读、无持久副作用**:models 可写用**试写探针**(mkstemp 写删,`finally` 清理);**不创建 models 目录**;不泄露任何密钥值(只报 vendor + 来源)。
- **退出码**:硬问题(models 存储不可用 / **config 损坏**)→ `1`;缺引擎/缺密钥/网络不可达/目录未建 = `info` → `0`。
- **config 直读**(不经会静默吞错的 `config.load()`)——否则漏掉"config 损坏"。
- 密钥只扫**已注册云端 vendor** 的精确 env 名(`<VENDOR>_API_KEY`/`_APP_KEY`/`_ACCESS_KEY`)。
- 模型计数/体积只统计 **store 管理的 sherpa meta**(`provider=="sherpa-onnx"`)。
- `--net`:`_probe(url, timeout=2.0)` 短超时无重试(urllib),不可达=info(永不 fail)。
- 提交 `git -c user.name="BolynWang" -c user.email="lm2039136@gmail.com"`,**显式 git add**,不 push。
- **测试** `PYTHONPATH=src python -m pytest ... -o addopts=""`。
- 契约见历史 spec：[`../specs/2026-07-07-w3b3-doctor-design.md`](../specs/2026-07-07-w3b3-doctor-design.md)。

---

## File Structure

- **Create** `src/asrkit/doctor.py` — `Check` + `diagnose(net)` + `_writable(path)` + `_probe(url, timeout)` + `_cloud_vendors()`。
- **Modify** `src/asrkit/cli.py` — `doctor` 子命令 + 渲染 + 退出码。
- **Create** `tests/test_doctor.py`。
- **Modify** `docs/usage.md`、`CHANGELOG.md`。

---

## Task 1: `doctor.py`(diagnose + 探针)

**Files:**
- Create: `src/asrkit/doctor.py`
- Test: `tests/test_doctor.py`

**Interfaces:**
- Produces: `Check(name, status, detail)`(status ∈ ok/info/fail);`diagnose(net=False) -> list[Check]`;`_writable(path)`、`_probe(url, timeout=2.0)`(monkeypatch 点)。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_doctor.py
import json
import os

import pytest

from asrkit import doctor


def _by(checks, name):
    return next((c for c in checks if c.name == name), None)


def test_diagnose_core_checks():
    cs = doctor.diagnose()
    names = {c.name for c in cs}
    assert {"asrkit", "python", "models-dir", "config"} <= names
    assert any(n.startswith("engine:") for n in names)


def test_keys_no_leak(tmp_path, monkeypatch):
    cfg = tmp_path / "config.json"
    cfg.write_text(json.dumps({"keys": {"dashscope": {"api_key": "sk-SECRET-123"}},
                               "defaults": {}, "settings": {}}))
    monkeypatch.setenv("ASRKIT_CONFIG", str(cfg))
    cs = doctor.diagnose()
    assert "sk-SECRET-123" not in "\n".join(c.detail for c in cs)   # 不泄露
    k = _by(cs, "key:dashscope")
    assert k and "present" in k.detail


def test_unrelated_env_not_reported(monkeypatch):
    monkeypatch.setenv("FOO_API_KEY", "x")            # 非注册 vendor
    assert not any(c.name == "key:foo" for c in doctor.diagnose())


def test_models_dir_unwritable_fail(monkeypatch):
    monkeypatch.setattr(doctor, "_writable", lambda p: False)
    md = _by(doctor.diagnose(), "models-dir")
    assert md and md.status == "fail"


def test_models_dir_not_created_is_info(tmp_path, monkeypatch):
    monkeypatch.setenv("ASRKIT_MODELS_ROOT", str(tmp_path / "sub" / "models"))  # 不存在,父可写
    md = _by(doctor.diagnose(), "models-dir")
    assert md and md.status == "info"


def test_config_corrupt_fail(tmp_path, monkeypatch):
    cfg = tmp_path / "config.json"
    cfg.write_text("{ not valid json ")
    monkeypatch.setenv("ASRKIT_CONFIG", str(cfg))
    c = _by(doctor.diagnose(), "config")
    assert c and c.status == "fail"


def test_net_probe_monkeypatched(monkeypatch):
    monkeypatch.setattr(doctor, "_probe", lambda url, timeout=2.0: False)
    net = [c for c in doctor.diagnose(net=True) if c.name.startswith("net:")]
    assert net and all(c.status == "info" for c in net)   # 不可达=info,不 fail


def test_writable_chmod_posix(tmp_path):
    if not hasattr(os, "geteuid") or os.name == "nt" or os.geteuid() == 0:
        pytest.skip("posix non-root only")
    ro = tmp_path / "ro"
    ro.mkdir()
    os.chmod(ro, 0o500)
    try:
        assert doctor._writable(str(ro)) is False
    finally:
        os.chmod(ro, 0o700)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=src python -m pytest tests/test_doctor.py -o addopts="" -v`
Expected: FAIL(`ModuleNotFoundError: asrkit.doctor`)

- [ ] **Step 3: 实现**

```python
# src/asrkit/doctor.py
"""asrkit doctor:离线体检 + opt-in 网络。只读、无持久副作用;不泄露密钥值。

diagnose(net=False) -> list[Check];cli 渲染 + 据 fail 定退出码。
硬失败(fail → 非零):models 存储不可用、config 损坏。缺引擎/密钥/网络 = info。
"""
from __future__ import annotations

import json
import os
import sys
import tempfile
from dataclasses import dataclass
from typing import List


@dataclass
class Check:
    name: str
    status: str   # "ok" | "info" | "fail"
    detail: str


def _writable(path: str) -> bool:
    """试写探针:向 path(不存在则向最近存在的祖先)写一个临时文件再删。无持久副作用。"""
    probe = path
    while probe and not os.path.isdir(probe):
        parent = os.path.dirname(probe.rstrip(os.sep))
        if parent == probe:
            break
        probe = parent
    if not os.path.isdir(probe):
        return False
    try:
        fd, tmp = tempfile.mkstemp(prefix=".asrkit_doctor_", dir=probe)
    except OSError:
        return False
    os.close(fd)
    try:
        os.unlink(tmp)
    except OSError:
        pass
    return True


def _probe(url: str, timeout: float = 2.0) -> bool:
    """网络可达:短超时 HEAD,失败退回小 Range GET;无重试。"""
    import urllib.request
    for method in ("HEAD", "GET"):
        try:
            req = urllib.request.Request(
                url, method=method,
                headers={"User-Agent": "asrkit", "Range": "bytes=0-0"})
            with urllib.request.urlopen(req, timeout=timeout) as r:
                return getattr(r, "status", 200) < 500
        except Exception:
            continue
    return False


def _cloud_vendors() -> list:
    from . import registry
    return sorted({m.vendor for m in registry.list_metas() if m.source == "cloud" and m.vendor})


def diagnose(net: bool = False) -> List[Check]:
    from . import __version__, config, engines, registry, store
    out: List[Check] = []

    out.append(Check("asrkit", "ok", __version__))
    out.append(Check("python", "ok", sys.version.split()[0]))

    # 引擎(仅包存在;sherpa 另需 numpy/soundfile/soxr)
    for name, (mod, extra) in engines.ENGINES.items():
        if engines.is_installed(name):
            out.append(Check(f"engine:{name}", "ok", "package present"))
        else:
            out.append(Check(f"engine:{name}", "info", f"not installed — pip install asrkit[{extra}]"))

    # 密钥(只报 vendor + 来源,绝不打印值)
    for v in _cloud_vendors():
        srcs = []
        if config.get_creds(v):
            srcs.append("keystore")
        vp = v.upper()
        if any(os.environ.get(f"{vp}_{s}") for s in ("API_KEY", "APP_KEY", "ACCESS_KEY")):
            srcs.append("env")
        out.append(Check(f"key:{v}", "info",
                         "present (" + "+".join(srcs) + ")" if srcs else "none"))

    # models 目录(试写探针 + 只统计 sherpa 管理的)
    root = store.models_root()
    if _writable(root):
        managed = [m for m in registry.list_metas() if m.provider == "sherpa-onnx"]
        installed = [m for m in managed if store.is_installed(m)]
        size = sum(store.dir_size(m) for m in installed)
        exists = os.path.isdir(root)
        state = "writable" if exists else "not created yet; created on first pull"
        out.append(Check("models-dir", "ok" if exists else "info",
                         f"{root} ({state}; {len(installed)} models, {size >> 20}MB)"))
    else:
        out.append(Check("models-dir", "fail", f"{root} not writable"))

    # config 完整性(直读,不经会吞错的 load)
    p = config.path()
    if not os.path.isfile(p):
        out.append(Check("config", "info", f"no config yet ({p})"))
    else:
        try:
            with open(p, encoding="utf-8") as f:
                d = json.load(f)
            if not isinstance(d, dict):
                raise ValueError("not an object")
            de = (d.get("defaults") or {}).get("engine") or "local"
            mr = (d.get("settings") or {}).get("models_root") or "(default)"
            out.append(Check("config", "ok", f"{p} (default-engine={de}, models-root={mr})"))
        except Exception as e:
            out.append(Check("config", "fail", f"config corrupt: {p} ({type(e).__name__})"))

    # 网络(opt-in;不可达=info,永不 fail)
    if net:
        metas = registry.list_metas()
        dl = next((m.download_url for m in metas if m.download_url), None)
        if dl:
            ok = _probe(dl)
            out.append(Check("net:download", "ok" if ok else "info",
                             ("reachable" if ok else "unreachable") + f" ({dl.split('/')[2]})"))
        cloud = next((m for m in metas if m.source == "cloud" and m.default_base_url), None)
        if cloud:
            ok = _probe(cloud.default_base_url)
            out.append(Check(f"net:{cloud.default_base_url.split('/')[2]}", "ok" if ok else "info",
                             "reachable" if ok else "unreachable"))
    return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=src python -m pytest tests/test_doctor.py -o addopts="" -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/asrkit/doctor.py tests/test_doctor.py
git -c user.name="BolynWang" -c user.email="lm2039136@gmail.com" commit -m "feat(doctor): diagnose 离线体检(试写探针/config 直读/密钥只报 vendor)+ --net 探针"
```

---

## Task 2: cli `doctor` 子命令 + 退出码

**Files:**
- Modify: `src/asrkit/cli.py`
- Test: `tests/test_doctor.py`

**Interfaces:**
- Consumes: `doctor.diagnose(net)`(Task 1)。
- Produces: `asrkit doctor [--net]` 渲染 + 退出码(fail→1)。

- [ ] **Step 1: 写失败测试(追加到 `tests/test_doctor.py`)**

```python
from asrkit import cli


def test_cli_doctor_ok(tmp_path, monkeypatch, capsys):
    monkeypatch.setattr("asrkit.doctor._writable", lambda p: True)
    monkeypatch.setenv("ASRKIT_CONFIG", str(tmp_path / "none.json"))   # 不存在 → info
    rc = cli.main(["doctor"])
    out = capsys.readouterr().out
    assert rc == 0 and "asrkit:" in out


def test_cli_doctor_nonzero_on_fail(monkeypatch):
    monkeypatch.setattr("asrkit.doctor._writable", lambda p: False)
    assert cli.main(["doctor"]) == 1


def test_cli_doctor_net(monkeypatch, capsys):
    monkeypatch.setattr("asrkit.doctor._probe", lambda url, timeout=2.0: True)
    rc = cli.main(["doctor", "--net"])
    assert rc in (0, 1)
    assert "net:" in capsys.readouterr().out
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=src python -m pytest tests/test_doctor.py -k cli -o addopts="" -v`
Expected: FAIL(无 doctor 子命令)

- [ ] **Step 3: 实现**

`cli.py` 加 `doctor` 子命令(在 serve 附近):
```python
    dp = sub.add_parser("doctor", help="diagnose install/config/engines/keys (add --net for reachability)")
    dp.add_argument("--net", action="store_true",
                    help="also check network reachability (download source / cloud)")
```
加处理块(放在 serve 处理块附近):
```python
    if a.cmd == "doctor":
        from . import doctor
        marks = {"ok": "✓", "info": "○", "fail": "✗"}
        checks = doctor.diagnose(net=a.net)
        for c in checks:
            print(f"{marks.get(c.status, ' ')} {c.name}: {c.detail}")
        return 1 if any(c.status == "fail" for c in checks) else 0
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `PYTHONPATH=src python -m pytest tests/ -o addopts="" -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/asrkit/cli.py tests/test_doctor.py
git -c user.name="BolynWang" -c user.email="lm2039136@gmail.com" commit -m "feat(cli): doctor 子命令(渲染 ✓/○/✗ + 硬问题非零退出)"
```

---

## Task 3: 文档(usage + CHANGELOG)

**Files:**
- Modify: `docs/usage.md`、`CHANGELOG.md`

- [ ] **Step 1: 更新 `docs/usage.md`**

加"体检 doctor"小节:`asrkit doctor`(离线:引擎/密钥/models 目录/config)、`asrkit doctor --net`(加网络可达);说明退出码(硬问题非零、缺引擎/密钥是信息);强调不泄露密钥、只读不修。

- [ ] **Step 2: 追加 `CHANGELOG.md` 的 `[Unreleased]` `### 新增`(不改版本号)**

```markdown
- **`asrkit doctor`**:一条命令体检安装/配置——引擎装没装、密钥配没配(只报有无,不泄露)、`~/.asrkit/models` 可写否、config 完整否;`--net` 加下载源/云端可达检查。硬问题(目录不可写/config 损坏)退出非零。
```

- [ ] **Step 3: 提交**

```bash
git add docs/usage.md CHANGELOG.md
git -c user.name="BolynWang" -c user.email="lm2039136@gmail.com" commit -m "docs(w3b3): doctor 用法 + CHANGELOG"
```

---

## Task 4: 收尾验证(ruff/mypy/全量)

**Files:** 无

- [ ] **Step 1: lint + 类型 + 全量**

Run:
```
ruff check src tests
mypy
PYTHONPATH=src python -m pytest tests/ -o addopts="" -q
```
Expected: ruff All checks passed;mypy Success;pytest 全绿。

- [ ] **Step 2: 有报错则 inline 修掉后重跑**(常见:未用 import `mod`、类型标注)

- [ ] **Step 3: 提交(若有修改)**

```bash
git add -u
git -c user.name="BolynWang" -c user.email="lm2039136@gmail.com" commit -m "chore(w3b3): lint/type 收尾"
```

---

## Self-Review 记录

- **Spec 覆盖**:Check+diagnose(版本/引擎/密钥/models-dir/config)+ 试写探针 + config 直读 + 密钥只扫注册 vendor + --net 探针(T1);cli 子命令 + 退出码(T2);docs(T3);验证(T4)。✅
- **Codex 修订全含**:`_writable` 试写(非 os.access)、config 直读列 fail、密钥精确 vendor env、`_probe` timeout=2.0 无重试、模型只计 sherpa 管理、引擎"包存在"措辞、便携 + POSIX-guarded 可写测试。
- **安全**:密钥只报 vendor+来源(test_keys_no_leak 钉死不泄露);无关 env 不误报。
- **类型一致**:`doctor.Check`、`diagnose(net)`、`_writable`、`_probe(url, timeout)` 跨任务一致。
